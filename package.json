{
  "name": "rolecall",
  "version": "1.0.0",
  "private": true,
  "description": "RoleCall — finds job listings matching your profile, emails you a digest, and prepares application drafts. Deploys to Cloudflare Workers.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
